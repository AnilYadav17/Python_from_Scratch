

Modes:
Read(r) :-  We can not write in it.
            Default mode if no mode is given.
    
Append(a) : It open the file for adding data at the end.
            It does not remove existing content , it creates file if not exist.

